Two simple scripts to assis users with log knowledge of git to syncronise their repositories between environments.
 - "Check my code is saved.cmd" is the entry point to run. It syncronize the scripts with github
 - "sync-check-gui.ps1" is the actual script that check if one or more repositories needs to by synched (changed files, commits not pushed, etc.) It does NOT handle merge conflicts!
